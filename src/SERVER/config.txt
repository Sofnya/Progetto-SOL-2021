SOCK_NAME=default_address
CORE_POOL_SIZE=8
MAX_POOL_SIZE=-1
MAX_FILES=100
MAX_MEMORY=100M
ENABLE_COMPRESSION=TRUE
VERBOSE_PRINT=TRUE
LOG_FILE=log
# alternatives are: RANDOM*, FIFO, LIFO*, LRU, MRU*, LU, MU*, SMOL, BIGG where ones with a * are probably best avoided.
POLICY=LRU